const NS_RESIDENT = 'resident';

const SET_LOADING = `${NS_RESIDENT}/setLoading`;

const SAVE_LOADING = `${NS_RESIDENT}/saveLoading`;

const GET_ALL_USER = `${NS_RESIDENT}/getAllUser`;

const SAVE_ALL_USER = `${NS_RESIDENT}/saveAllUser`;

const GET_DETAIL_USER = `${NS_RESIDENT}/getDetailUser`;

const CREATE_USER_RESIDENT = `${NS_RESIDENT}/createUserResident`;

const UPDATE_USER_RESIDENT = `${NS_RESIDENT}/updateUserResident`;

export {
  SET_LOADING,
  SAVE_LOADING,
  GET_ALL_USER,
  SAVE_ALL_USER,
  CREATE_USER_RESIDENT,
  GET_DETAIL_USER,
  UPDATE_USER_RESIDENT,
};
