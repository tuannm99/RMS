const NS_AUTH = 'auth';

const SET_LOADING = `${NS_AUTH}/setLoading`;

const SAVE_LOADING = `${NS_AUTH}/saveLoading`;

const LOGIN_REQUEST = `${NS_AUTH}/loginRequest`;

const SAVE_LOGIN_DATA = `${NS_AUTH}/saveLoginData`;

const SAVE_LOGIN_RES = `${NS_AUTH}/loginRes`;

const LOGOUT_REQUEST = `${NS_AUTH}/logoutRes`;

const SAVE_LOGOUT_REQUEST = `${NS_AUTH}/saveLogoutRes`;

export {
  SET_LOADING,
  SAVE_LOADING,
  LOGIN_REQUEST,
  SAVE_LOGIN_DATA,
  SAVE_LOGIN_RES,
  LOGOUT_REQUEST,
  SAVE_LOGOUT_REQUEST,
};
