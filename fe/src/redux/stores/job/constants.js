const NS_JOB = 'job';

const SET_LOADING = `${NS_JOB}/setLoading`;

const SAVE_LOADING = `${NS_JOB}/saveLoading`;

const GET_JOBS = `${NS_JOB}/getJob`;

const SAVE_JOBS = `${NS_JOB}/saveJob`;

export { SET_LOADING, SAVE_LOADING, GET_JOBS, SAVE_JOBS };
