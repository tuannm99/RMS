const tokenTypes = {
  ACCESS: 'access',
  REFRESH: 'refresh',
  RESET_PASSWORD: 'resetPassword',
  VERIFY_EMAIL: 'verifyEmail',
};

// TODO: fix role
const roles = ['employee', 'admin'];

module.exports = {
  tokenTypes,
};
